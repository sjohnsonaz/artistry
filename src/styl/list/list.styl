@require "list-settings.styl";

.list {
    list-style-type: none;

    & > li {
        border: 1px 0 1px 0 solid #000;
        padding: 10px;
        margin: 0;
        background-color: white;
    }
}